"""degreezeor — empirical, source-anchored scoring of public decisions.

The platform measures public *actions* against their *own stated objectives*,
relative to a defensible baseline, with transparent attribution and confidence,
and a fully auditable path from every score back to official sources.

It deliberately does NOT emit a default normative "good/bad politician" number;
the value layer is externalized to user-configurable weights with a neutral default.
"""

__version__ = "0.1.0"
